# ============================================================
# AI Career Copilot 后端环境变量
# 按环境复制对应模板为 backend/.env（.env 已在 .gitignore 中）
#
#   本地开发：cp env.development.example .env
#   线上部署：cp env.production.example .env
# ============================================================
#
# 两套数据库（同一 RDS 实例，库名不同）：
#   Node 认证 API (server/)  → gba_website
#   Python AI 后端 (backend/)  → ai_career_copilot
#
# RDS 连接信息（线上）：
#   host: rm-wz93273d48x7x6d5k5o.mysql.rds.aliyuncs.com
#   port: 3306
#   user: GBA_platform
#   charset: utf8mb4
#
# 详见 env.development.example 与 env.production.example
